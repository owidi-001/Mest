import 'package:flutter/material.dart';

class FavouriteFoods extends StatelessWidget {
  const FavouriteFoods({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("data"),);
  }
}
